# AXE NET PRO v1.47.5

## v1.47.5 · 스킬 포인트 요구량 전체 갱신
- 2026-08-27 제공 CSV 기준 24개 스킬 / 123개 확인 구간 반영
- 이전 값 8건 교정 및 누락 구간 보강
- 돌진 A → 9 수련서 5장으로 정정
- `아이언 주` 기존 오인 데이터를 제거하고 `악기연주`로 정리
- `-` / `현재 없음` 구간의 오래된 값이 남지 않도록 새 migration에서 대상 스킬을 재구성


## v1.47.4 · 추천세팅 작성 안정성 / 필터 / 가독성
- 추천세팅 작성·수정 내용을 브라우저 localStorage에 자동 임시저장하고 새로고침 후 복원합니다.
- 접두와 접미의 분류/검색 필터를 완전히 분리해 서로 다른 카테고리 조합을 자유롭게 구성할 수 있습니다.
- 게시글 상세 우측의 주의 옵션과 부위별 코멘트 글자 크기/간격을 키우고, 코멘트를 우측 요약 카드 안으로 이동해 빈 공간을 줄였습니다.
- 저장 성공 시 해당 작성 초안을 자동 삭제합니다.

## v1.47.3 · 모달 보호 + 브라우저 내비게이션 히스토리
- 추천세팅 작성/수정 등 INFO 모달은 바깥 영역을 클릭해도 닫히지 않습니다.
- 모달 닫기는 닫기/취소 버튼, ESC, 브라우저 뒤로가기로 제한했습니다.
- AXE NET의 상위 카테고리와 내부 탭 이동을 브라우저 History에 기록합니다.
- 브라우저 뒤로가기를 누르면 이전 AXE NET 카테고리/탭으로 이동하고, 내부 기록을 모두 소진한 뒤에만 이전 외부 사이트로 돌아갑니다.
- 추천세팅 게시글 목록↔상세 이동도 History에 포함했습니다.
- URL은 Vercel 재작성 설정이 필요 없는 hash route(`#/...`) 방식입니다.

# AXE NET v1.47.2

## v1.47.2 · 추천세팅 목록 밀도 / 슬롯 정렬
- 추천세팅 목록을 큰 카드형 2열에서 실제 게시판처럼 조밀한 1행 목록형으로 재구성
- 제목/요약, 태그, 작성자, 저장 수, 수정일을 한눈에 볼 수 있도록 정렬
- 게시글이 많아져도 세로 공간을 과도하게 차지하지 않도록 행 높이 축소
- 신발 슬롯을 하의와 같은 두 번째 행 시작점으로 맞춰 오와열 정렬


## v1.47.2 · 추천세팅 게시글 상세 / 개조서 선택 필터
- 추천세팅 목록과 게시글 상세를 분리했습니다. 목록에서 글을 누르면 해당 게시글만 열리고 `← 추천세팅 목록`으로 돌아갑니다.
- 게시글 상세에서는 인벤토리 장비창 왼쪽, 전체 옵션 요약을 오른쪽에 다시 배치해 한눈에 볼 수 있게 했습니다.
- 추천세팅 작성기의 각 부위에 `분류` + `개조서명/옵션 검색` 필터를 추가했습니다. 접두/접미 선택 목록은 필터 결과만 표시됩니다.
- 기존 데이터의 `겉옷/단독상의`를 겉옷 슬롯으로 정상 인식하도록 웹/저장 검증을 함께 보정했습니다.
- Supabase에서 `supabase/048_modbook_preset_outer_compat.sql`을 1회 실행해야 겉옷 저장 검증까지 정상화됩니다.

## 적용 순서
1. **047을 아직 실행하지 않았다면** 먼저 `supabase/047_modbook_preset_community.sql` 전체 실행
2. Supabase SQL Editor에서 `supabase/048_modbook_preset_outer_compat.sql` 전체 실행
3. 이 ZIP 전체를 현재 GitHub AXE NET 프로젝트에 덮어쓰기
4. PowerShell에서 아래 검사
   - `npm.cmd run audit:styles`
   - `npm.cmd run audit:ux`
   - `npm.cmd run build`
5. GitHub push 후 Vercel 배포

## 이전 버전 기록

## v1.47.0 · 추천세팅 게시판 / 내 프리셋
- 기존 고정 추천세팅 화면을 게시글 기반 추천세팅 커뮤니티 구조로 확장했습니다.
- 멤버는 제목, 설명, 태그, 부위별 코멘트를 작성할 수 있습니다.
- 겉옷/상의/하의/신발마다 현재 AXE NET에 등록된 개조서 중 해당 부위에 적용 가능한 접두/접미를 선택합니다.
- 개조서 옵션은 중복 입력하지 않고 기존 `info_modbooks` 데이터를 사용합니다.
- 게시글 상세에서는 기존 인게임형 장비 슬롯 + hover 최대옵 툴팁을 그대로 사용합니다.
- `★ 내 프리셋`으로 게시글을 개인 즐겨찾기에 저장할 수 있습니다.
- `복제해서 작성`으로 기존 세팅을 기반으로 자기 버전을 만들 수 있습니다.
- 작성자는 자신의 게시글을 수정/삭제할 수 있고, admin 멤버는 전체 게시글 관리가 가능합니다.
- 첫 게시글로 기존 `이동속도 1티어` AXE 추천안을 SQL에서 자동 이관합니다.
- Supabase 마이그레이션 `047_modbook_preset_community.sql`이 필요합니다.

## v1.46.4 · 팀복 실전 적용 / 추천세팅 Hover 안정화
- 사용자가 제공한 `팀복.zip` 원본 PNG 4종을 추천세팅 슬롯에 직접 적용했습니다.
- 겉옷/상의/하의/신발 이미지를 고해상도 원본으로 교체했습니다.
- 슬롯 간 마우스 이동 시 이전 tooltip hover가 남는 문제를 JS 기반 active hover로 보강했습니다.
- 겉옷/신발 tooltip이 보드 경계에서 잘리던 문제를 수정했습니다.
- 우측 옵션 요약과 tooltip 옵션 텍스트 크기를 키웠습니다.

## v1.46.3 변경사항
- 개조서 추천 장비 슬롯 이미지를 AXE 팀복 기준의 고해상도 PNG 자산으로 교체했습니다.
- 슬롯 이미지가 깨져 보이던 문제를 완화하도록 렌더링/프레임 스타일을 조정했습니다.
- 툴팁을 `pointer-events: none` 기반으로 변경해서 한 슬롯에서 다른 슬롯으로 마우스를 바로 옮겨도 즉시 인식되도록 수정했습니다.

# AXE NET v1.46.2

## v1.46.2 · 추천세팅 옵션 합산 정규화
- 추천세팅 우측 요약 헤더의 이동속도 총합 숫자를 제거하고 옵션표 안으로 이동
- 이동 속도 증가를 다른 옵션과 동일한 방식으로 총합 표시 (`+53%` 형태)
- `최대스태미나증가` / `최대 스태미나 증가`를 동일 옵션으로 정규화하여 합산
- 요약 옵션은 정규화된 표기명으로 출력하여 `최대 스태미나 증가 +600`처럼 한 줄로 표시
- 기존 Supabase에 오타 데이터가 남아 있어도 화면/합산은 정상 동작하며, 번들 import 원본도 표기를 정리


## v1.46.2 · 추천세팅 UI 가독성 개선
- 추천세팅 장비 이미지를 저해상도 스크린샷 기반 WEBP에서 깔끔한 범용 SVG 의상 아이콘으로 교체
- 툴팁을 불투명 패널로 변경하고 대비/폰트 크기/레이아웃을 강화
- 슬롯 hover 시 다른 슬롯/배경을 어둡게 처리해 현재 옵션에 집중되도록 조정



## v1.46.2 · 개조서 추천세팅
- 정보 메뉴에 `추천세팅` 탭을 추가했습니다.
- 실제 게임 인벤토리 배치를 참고한 장비 슬롯 UI를 사용합니다.
- 개조 가능한 4개 부위(겉옷/상의/하의/신발)만 활성 슬롯으로 표시합니다.
- PC에서는 장비에 마우스를 올리면, 모바일에서는 터치하면 개조서 툴팁을 확인합니다.
- 툴팁은 최대 옵션을 앞에 크게, 실제 옵션 범위를 괄호로 함께 표시합니다.
- 첫 프리셋은 `이동속도 1티어` AXE 추천 초안이며 기존 `info_modbooks` 데이터를 그대로 참조합니다.
- 추천 조합 정의는 `src/modules/info/modbookPresets.js` 한 파일에서 관리합니다.

## v1.45.1 · 스킬랭크 데이터 보강
- 2026-08-26 입수한 스킬랭크 표의 **확인된 숫자만** 반영합니다.
- 빈칸은 기존 값을 삭제하지 않습니다.
- `벌목 D → C`, `채광 D → C`의 10SP는 `재확인 필요` 메모를 남깁니다.
- `A → 9` 구간 및 `수련서 3장` 추가 요구사항을 저장합니다.
- 신규 확인: `아이언 주`, `전문가 치료(EMS)`, `몸 수색(경찰)`.
- Supabase SQL Editor에서 `supabase/046_skill_rank_update_20260826.sql`을 1회 실행해야 실제 운영 데이터에 반영됩니다.

## UI / IA REDESIGN
실사용 기능 이전 이후 첫 전체 UI 정돈 버전입니다. 데이터 구조나 Supabase RPC는 변경하지 않고, 화면의 정보 구조와 AXE 브랜드 표현을 재설계했습니다.

### 이번 버전 핵심
- 상단 1차 메뉴를 `홈 / 소식 / 정보 / 콘텐츠 / 운영` 5개로 축소
- `무법지대 / AXE TUBE`를 `콘텐츠` 2차 메뉴로 이동
- `공금 / 자산·계좌 / 멤버`를 `운영` 2차 메뉴로 이동
- 홈의 KPI/바로가기/공금 진행률 박스를 제거하고 `공지 3건 / 최근 공금 4건 / AXE TUBE 3건` 미리보기만 노출
- 홈 전체 폭을 배너/카테고리 축에 맞춰 정렬
- 공지사항 페이지의 헤더·탭·목록 폭을 같은 축으로 통일
- 정보 페이지의 헤더·탭 축을 운영 워크스페이스와 통일
- AXE TUBE의 영상/조회/추천/분류 KPI, Runtime Primary 안내, 관리자 운영 상태 패널 제거
- AXE TUBE 검색·필터 직후 바로 콘텐츠 시작
- AXE TUBE 카드 제목/작성자/메타 가독성 상향
- 기존 `axe-hero-premium.webp`를 활용한 와이드 브랜드 배너로 재설계

## 배포
기존 GitHub `NEW-AXE-NET` 저장소에 이 버전의 파일을 덮어쓰고 Push하면 Vercel 자동 배포됩니다.

## SQL
**없음.** Supabase SQL Editor에서 실행할 작업이 없습니다.

## 환경변수
**변경 없음.**

## AXE BOT
**변경 없음.**

자세한 UI 기준은 `docs/UI_REDESIGN_1.0.md`를 참고하세요.

## 이전 버전 유지사항
- v1.40.0 MEMBER ADMIN BRIDGE 유지
- 공금 AXE NET Supabase 런타임 유지
- AXE TUBE Supabase-first 런타임 유지
- 자산·계좌 / 무법지대 / 정보 / 공지 모듈 기능 로직 변경 없음


## v1.41.1 UI polish
- 브랜드 배너 높이와 로고 카피를 축소해 과한 인상을 줄였습니다.
- 홈 AXE TUBE 미리보기는 2개 대형 카드 중심으로 재구성했습니다.
- 콘텐츠/운영 2차 메뉴에서 좌측 보조 라벨(콘텐츠, 운영) 노출을 제거했습니다.


## v1.41.2 UI tuning
- 홈 AXE TUBE 미리보기를 과하지 않게 다시 축소하고, 2열 컴팩트 카드형으로 조정했습니다.
- 브랜드 배너 내부 AXE 로고가 과하게 커 보이던 문제를 줄이기 위해 배너 높이/배경 스케일/카피 크기를 추가 조정했습니다.


## v1.41.3 home balance tweak
- 홈 AXE TUBE 미리보기 노출 수를 3개에서 4개로 늘려 2x2 균형형 배치가 되도록 조정했습니다.


## v1.41.4 banner apply
- 사용자가 제공한 AXE 배너 이미지를 사이트 배너 영역에 맞게 크롭 적용했습니다.
- 기존 배너 오버레이 문구/메타 표시는 제거하고, 배너 이미지 자체가 자연스럽게 보이도록 정리했습니다.
- 홈 AXE TUBE 4개 미리보기 구성은 유지됩니다.

## v1.42.0 QUICK ACCESS
- 로그인 멤버별 개인 바로가기 기능을 추가했습니다.
- 상단 `⚡ 바로가기`에서 최대 6개까지 등록/수정/삭제/순서변경이 가능합니다.
- 표시 이름은 사용자가 직접 정할 수 있습니다. 예: `공금내기` → `운영 > 공금 > 공금납부`.
- 공금 납부/공금내역/브리핑맵/플리카계좌 등 세부 기능으로 바로 이동합니다.
- 홈에서는 등록된 바로가기를 얇은 한 줄 형태로만 노출해 기존 미리보기 중심 홈 구성을 해치지 않습니다.
- 운영진 전용 기능은 현재 관리자 권한이 있는 계정에게만 바로가기 대상으로 노출됩니다.
- 개인 설정은 `new_axe_net.member_shortcuts`에 저장되어 다른 기기에서도 유지됩니다.

### 적용
1. Supabase SQL Editor에서 `supabase/044_member_shortcuts.sql` 전체 실행
2. 이 버전을 GitHub `NEW-AXE-NET`에 덮어쓰고 Push
3. Vercel 자동 배포 완료 후 멤버 로그인 → 상단 `⚡ 바로가기` → `관리`에서 등록

### SQL
필요: `044_member_shortcuts.sql`

### 환경변수
변경 없음. 기존 `SUPABASE_SERVICE_ROLE_KEY` 서버 환경변수를 그대로 사용합니다.

### AXE BOT
변경 없음.


## v1.42.1 QUICK ACCESS MODAL FIX
- 상단 바로가기 관리창이 화면 위로 잘리던 문제를 수정했습니다.
- 원인은 `backdrop-filter`가 적용된 sticky 상단바 내부에서 `position: fixed` 모달을 렌더링해, 일부 브라우저에서 상단바를 기준으로 고정 위치가 계산되던 구조였습니다.
- 바로가기 관리창을 `body` 직속 전용 portal root로 분리해 항상 브라우저 viewport를 기준으로 표시되도록 변경했습니다.
- 낮은 화면/모바일에서는 상단 여백을 유지하고 내부 스크롤로 전체 항목에 접근할 수 있도록 보강했습니다.

### 적용
- SQL 없음
- 환경변수 변경 없음
- GitHub `NEW-AXE-NET` 저장소에 이 버전 파일을 덮어쓰고 Push하면 됩니다.
